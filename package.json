{
  "name": "analects-2-17",
  "private": true,
  "version": "0.1.0",
  "description": "Analects 2.17 — a multi-factor investment ranking platform for mid/large-cap equities",
  "workspaces": [
    "shared",
    "web",
    "functions"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web",
    "build:shared": "npm run build --workspace shared",
    "build:functions": "npm run build --workspace functions",
    "test": "npm run test --workspace shared --if-present && npm run test --workspace functions --if-present && npm run test --workspace web --if-present",
    "test:e2e": "playwright test",
    "lint": "npm run lint --workspace web --if-present && npm run lint --workspace functions --if-present",
    "emulators": "firebase emulators:start"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
